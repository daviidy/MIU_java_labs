import java.util.*;

public class Application {

	public static void main(String[] args) {
		Scanner myObj = new Scanner(System.in);  // Create a Scanner object
	    System.out.println("Enter number of packages you want to send");

	    String number = myObj.nextLine();
	    
	    for (int i = 0; i < Integer.parseInt(number); i++) {
	    	System.out.println("Enter description of the package number " + i+1);

		    String name = myObj.nextLine();
		    
		    System.out.println("Enter weight of the package number " + i+1);

		    double weight = Double.parseDouble(myObj.nextLine());
		}

	}

}
